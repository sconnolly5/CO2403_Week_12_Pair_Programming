namespace CO2403_Week_12_Pair_Programming
{
    /// <summary>
    /// This enum should provide the user with different options they are able to select on the menu.
    /// </summary>
    internal enum EMenuChoice
    {
        None = 0,       // This does not need to be shown to the user as it represents an unitinitialised state
        Fizzbuzz = 1,
        Fibonacci = 2,
        VowelCounter = 3
    }
}
